#include <stdio.h>
int main(int argc, char const *argv[])
{
    int x;
    float y;
    scanf("%d",&x);
    scanf("%f",&y);
    if(x+0.5<y && x%5==0)
    {
        printf("%0.2f",y-x-0.50);
    }
    else
    {
        printf("%0.2f",y);
    }
    return 0;
}
